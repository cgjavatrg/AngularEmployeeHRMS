import { DepartmentProducerS } from './department-producer-s';

describe('DepartmentProducerS', () => {
  it('should create an instance', () => {
    expect(new DepartmentProducerS()).toBeTruthy();
  });
});
